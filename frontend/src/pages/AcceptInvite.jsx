import { useEffect,useState } from 'react';import { useNavigate,useParams } from 'react-router-dom';import { acceptInvitation } from '../api/channels';
export default function AcceptInvite(){const {token}=useParams(),nav=useNavigate(),[state,setState]=useState('Aceptando invitación...');useEffect(()=>{acceptInvitation(token).then(()=>{setState('Listo, ya tienes acceso.');setTimeout(()=>nav('/app'),900)}).catch(e=>setState(e.response?.data?.message||'No se pudo aceptar la invitación.'))},[token]);return <div className="dc-invite-page"><h1>DrawCast // COLABORACIÓN</h1><p>{state}</p></div>}
